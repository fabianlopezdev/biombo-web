---
const { image } = Astro.props
---

{
  /* TODO: Uncomment Image component when switching to SSG
<Image
  src={image.asset.url}
  alt=""
  style="border-start-start-radius: 0.621875rem; border-end-start-radius: 0.621875rem;"
  class="slider__img"
  width={image.asset.metadata?.dimensions.width ?? 600}
  height={image.asset.metadata?.dimensions.height ?? 800}
  densities={[1, 1.5, 2]}
  format="webp"
  quality={80}
  loading="lazy"
/> */
}
{/* Temporary img tag for SSR - remove when switching to SSG */}
<img
  src={image.asset.url}
  alt=""
  style="border-start-start-radius: 0.621875rem; border-end-start-radius: 0.621875rem;"
  class="slider__img"
  width={image.asset.metadata?.dimensions.width ?? 600}
  height={image.asset.metadata?.dimensions.height ?? 800}
  loading="lazy"
/>

<style>
  .slider__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
</style>
